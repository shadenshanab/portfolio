# Copy this file to .dev.vars (gitignored) and fill in a real token for local
# `wrangler dev` testing. Production uses `npm run secret:hf` instead — never
# a file — see README.md in this directory.
HF_TOKEN=hf_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
